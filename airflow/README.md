# Airflow in Docker Compose 
```sh
.
├── dags
│   └── airflow_etl_pipeline.py
├── Dockerfile
└── docker-compose.yaml
```

1. Start Airflow:

```
docker-compose up
```

2. Visit: http://localhost:8080


